@model IEnumerable<WebApplication23.Models.Resturant>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ResturantName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ResturantName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ResturantId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ResturantId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ResturantId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
